version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - yarn exec amplify init
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
